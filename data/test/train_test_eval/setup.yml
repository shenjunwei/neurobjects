name: ge4
spikes_dir:
contacts_file:
output_dir: data/test/train_test_eval/datasets

params:
  areas: [hp,s1,v1,"*"]
  bin_size: 0.250
  window_width: 10

datasets:
  - name: ge4
    rounds: 1
    train_ratio: 0.8

    1_vs_n:
      labels: [ball, brush, food, urchin]
      class_ratio: 2
      total_positives: 70
