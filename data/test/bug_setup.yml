name: test_data
spikes_dir: data/test/spikes/
contacts_file: data/test/bug_contacts.txt

output_dir: data/test/datasets/test_data/

params:
  areas:
    - HP_12b
  bin_size: 0.250  # must not be an integer
  window_width: 2

datasets:
  - name: ge4
    rounds: 1
    train_ratio: 0.8

    1_vs_n:
      labels: [ball, food, urchin]
      class_ratio: 2
      total_positives: 3

